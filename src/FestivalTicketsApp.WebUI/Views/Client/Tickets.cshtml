@model FestivalTicketsApp.WebUI.Models.Client.ClientTicketsViewModel

<div class="container h-100 d-flex flex-column">
    <partial name="TicketsPartials/FilterPart" model="Model" />
    <h2 class="fw-bold mb-3">Your tickets</h2>
    <partial name="TicketsPartials/TicketListPart" model="Model.PurchasedTickets" />
    <partial name="Pagination" model="@(new PaginationModel(Model.CurrentPageNum, Model.NextPagesAmount))" />
</div>
